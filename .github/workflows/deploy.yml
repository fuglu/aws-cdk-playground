name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Echo repository name
      run: echo ${{ format(github.repository, "fuglu/") }}
